# 汲作 v0.2.16

- 修复：上一版的表格修复在多数真实网页上没有生效——表格外面套一层容器就会塌回一行连读的字。现在真的按行列显示了。
- 修复：有序列表在阅读区会从 1 重新数起，接续编号（比如从第 3 步开始的清单）被抹掉。现在保留原文的编号。
- 表格单元格里的竖线和反斜杠不会再被吃掉或拆错格。

上一版（v0.2.15）说好的「表格重新按行列显示」，实际只在少数结构简单的页面上成立。这一版补齐。
