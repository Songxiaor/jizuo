# LinkDigest 项目规则

## 定位

本项目同时是一个准备公开发布的桌面产品和 Syc 的完整 APP 开发学习工程。实现不仅要能运行，还要留下初学者可理解、可复现、可验证的决策记录。

## 双轨交付

每个里程碑必须同时交付：

1. 可运行的软件或可观察的原型。
2. 自动化测试或明确的人工验收证据。
3. 面向初学者的“场景 → 角色与交接 → 工作流 → 工具协同”说明。
4. 本阶段的决策、失败、恢复方式和下一步。

## 任务级学习契约

开发任务不能只交付代码。除纯文字修正、格式调整等不引入新概念的微小任务外，每个任务必须使用 `docs/TASK_TEMPLATE.md` 的学习闭环：

1. **任务开始前**：先用“场景 → 角色与交接 → 工作流 → 工具协同”解释任务；列出本次最多 3–5 个新名词。
2. **名词第一次出现时**：必须说明人话定义、生活类比、项目位置、为什么需要以及不使用会怎样，不能默认 Syc 已经理解。
3. **开发过程中**：阶段更新要说明当前组件的职责和交接物，不能只报告文件名、命令或百分比。
4. **理解发生在过程中**：解释必须跟随正在实施的组件出现，不能把学习集中成开发结束后的作业、考试或答题门槛。
5. **可选跟做与深挖**：可以给 Syc 一个 5–15 分钟可共同观察的动作，但它是辅助理解，不是关闭任务的前置条件；Syc 想深入时可单独开小窗口学习，主开发流程默认继续。
6. **任务关闭前**：完成工程验收，确认过程讲解已经交付，并更新 `docs/LEARNING_LOG.md`；新名词按需进入 `docs/GLOSSARY.md`。

学习深度分四级：L1 认识、L2 定位、L3 共同观察验证、L4 修改。它描述本项目提供到什么讲解深度，不是对 Syc 掌握程度的考试。核心架构概念应提供到 L3；一次性工具名默认只讲到 L1，避免信息过载。

不得要求 Syc 先独立学习、复述答案或完成课后验证，才继续开发或标记任务完成。只有当 Syc 明确表示某个未知点会影响当前产品决策时，才暂停主流程补充解释。

### 任务完成定义

- 工程完成：功能或文档产物存在，测试或检查有证据，失败和回滚路径明确。
- 讲解完成：AI 已在实施过程中解释组件的场景、角色、交接、选择原因和失效表现，并留下可回看的入口。
- 完整完成：工程完成、过程讲解已交付且学习日志已更新；不以 Syc 交作业或答题作为条件。

## 产品硬约束

- local-first：P0 的链接、原文、摘要和历史默认只保存在本机。
- macOS-native：P0 桌面 APP 使用 Swift + SwiftUI，只有明确的系统能力缺口才局部桥接 AppKit；不得为未验证的 Windows 需求改回 Web 桌面壳。
- Chromium-first：浏览器扩展优先支持 Chrome、Brave、Edge；Safari 不进入 P0。
- cross-language contract：Swift APP 与 TypeScript 扩展只通过版本化、语言中立的 JSON 合同交接；TypeScript interface 不能单独充当跨语言真相源。
- 当前页面优先：登录受限页面先读取用户已打开、已渲染的 DOM，不默认导入完整浏览器 Cookie。
- Provider-neutral：不得绑定单一第三方端点；至少支持自定义 OpenAI-compatible Base URL、API Key、模型名和 API 模式。
- 可解释失败：抓取、模型、网络、权限和平台限制必须分层呈现，不能只显示“失败”。
- 用户数据可迁移：历史和结果至少支持 Markdown、纯文本和 JSON 导出。
- cloud-deferred：账号、同步、托管模型、服务端与百万容量实施不进入 P0；远期文档不得反向扩大当前范围。

## 安全与合规

- 永远不要把 API Key、Cookie、Token、私钥或真实账号数据写入仓库、日志、截图和测试夹具。
- Cookie 权限必须按当前域名、用户主动触发、可撤销、短时使用；不得静默读取整个浏览器 Cookie 数据库。
- 不实现批量账号采集、绕过付费墙、绕过验证码或规避平台访问控制。
- 使用第三方开源代码前核对许可证；GPL、AGPL 和“仅限非商业学习”代码不得未经评估直接合入准备商业上架的产品。
- 购买服务器、域名、证书、签名服务，创建账号，提交商店或发布内容前必须再次获得 Syc 确认。

## 工程规则

- 先实现一条端到端可用链路，再扩平台。
- 共享协议先定义类型和失败语义，再连接桌面端、扩展和提取器。
- 新增平台适配器必须带至少一个脱敏夹具和成功、登录受限、平台变化三个测试场景。
- 依赖当前平台行为的结论先现场验证；不根据旧文档承诺成功率。
- 修改代码后运行与风险相称的 lint、类型检查、单元测试和端到端测试。
- 未经明确要求，不自动提交、推送、创建 remote、发布构建或安装系统级依赖。

## 文档归属

- `docs/PRD.md`：产品范围、状态、验收标准。
- `docs/ARCHITECTURE.md`：当前架构和组件边界。
- `docs/LEARNING_GUIDE.md`：学习路线和阶段复盘模板。
- `docs/TASK_TEMPLATE.md`：每个非平凡开发任务的学习与工程双验收模板。
- `docs/GLOSSARY.md`：只记录项目真实出现过的名词和 Syc 需要达到的理解等级。
- `docs/LEARNING_LOG.md`：记录每个任务已交付的过程讲解、可选跟做入口和 Syc 主动提出的待解释点。
- Project Brain：耐久决策与历史反转；必须通过 Brain CLI 读写，禁止手改 `brain/`。
