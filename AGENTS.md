# LinkDigest 项目规则

## 定位

本项目是一个准备公开发布的桌面产品。实现应当可运行、可维护、可验证，并保持对初学者友好的代码与必要说明。

需要解释时使用清晰的人话和项目内真实例子；解释服务于当前决策和排障，不作为开始开发或完成任务的前置门槛。

## 协作节奏与任务分级

目标：**简单任务直接完成，普通开发保持顺畅，高风险改动谨慎验证。** 默认单 Agent、小范围修改，不把正常编码自动升级成规划仪式、文档工程或多 Agent 工作流。

### 启动方式

- 自包含的小任务只读取直接相关的代码和测试，不强制预读完整交接文档、Brain 或长篇项目文档。
- 任务依赖近期运行状态、未完成工作或历史坑点时，读取最新的 `docs/交接文档-*.md`。
- 涉及产品范围、架构边界或历史反转时，再通过 Brain CLI 读取相关 Project Brain 页面；不为普通 bug 修复做宽泛背景加载。
- 修改前先确认相关实现、当前 dirty 状态和实际运行版本，避免覆盖他人改动或修错旧产物。

以下已知工程坑在相关任务中直接复用，不重复探路：

- `apps/browser-extension/src/content/extract.ts` 有 `extractCurrentPage`（可测试版）与 `extractPageInIsolatedWorld`（生产自包含版）**两份拷贝**，抽取/清洗/平台分支必须两份都改。
- 平台枚举改动链：`contracts/*.schema.json` → `pnpm generate:validator` → `scripts/sync-contracts.sh`。
- 打包需把 SwiftPM 资源包补到 `.app` 根目录再 ad-hoc 重签，否则换机崩。

### Project Brain

- 根目录 `BRAIN.md` 是项目记忆协议入口，`brain/` 是唯一 Project Brain；不得为 Claude、Codex 或其它 Agent 创建互相分叉的项目记忆。
- 简单、自包含且能从代码直接判断的任务不必读取 Brain。涉及产品范围、架构、技术选型、长期约束、历史反转，或当前需求可能与既有决策冲突时，先用 Brain CLI 读取相关 active 页面。
- Brain 只保存半年后仍重要、且难以从代码和测试重新推导的决策与理由；普通实现细节、临时排障过程和可由 Git 重建的信息不写入 Brain。
- 对 `brain/` 的读取和写入全部通过 Brain CLI；严禁手改。改变既有结论时必须通过 CLI 追加 reversal 或归档旧页面，保留决策演变。
- `docs/PRD.md` 保存完整产品行为，`docs/ARCHITECTURE.md` 保存当前组件边界，Brain 保存长期决策原因与反转条件，三者不复制整段正文。

### 标准开发流程

1. 读取直接相关的实现、测试和当前状态。
2. 只有当不同选择会明显改变产品行为、风险或工作量时才向 Syc 提问；其余使用最小合理假设继续。
3. 按现有模式做最小正确修改，不顺手重构、不建立额外机制。
4. 运行与改动风险相称的 lint、类型检查或聚焦测试；不默认跑无关全量套件。
5. 影响 UI、扩展、Host 或真实运行链路时，在实际 App/浏览器工作流中做一次必要验收。
6. 结论先行，汇报修改结果、关键证据、已知边界和最小下一步。

### 任务分级

- **简单任务**：文案、样式、选择器、局部逻辑、小型测试或单文件修复。当前 Agent 直接完成，只做聚焦验证，不写计划、不做额外文档、不启动多 Agent。
- **普通开发**：跨少量文件的功能或 bug 修复，但不涉及高风险边界。当前 Agent 直接实施，运行相关测试；需要真实运行证据时部署一次并验收一次。
- **高风险任务**：native messaging/Host/manifest、打包器、`contracts/*.schema.json`、跨语言契约、安全/权限/凭据、数据迁移、破坏性操作或架构级改动。先给简短计划；修改重要配置或数据前创建时间戳备份；完成后做针对性审查和端到端验收。
- 拿不准时根据“失败后影响范围和恢复成本”判断，不因不确定本身自动升级；只有关键选择无法从现场证据确认时才暂停询问。
- 仅在 Syc 明确要求，或任务确实可拆成互不冲突的独立工作时使用多 Agent。
- 多 Agent 默认并发 1，确有独立工作时最多并发 2；禁止子 Agent 再派生。每次都要验收其结果，出现报告失真、越界或连续两轮无进展时立即停用该通道，改由当前 Agent 直接完成并说明原因。

### 与 Syc 的迭代节奏

- 简单任务保持简洁；复杂任务只在阶段变化、发现新风险或需要 Syc 决策时更新。
- 默认改完后在这台开发机上打开实际 App 让 Syc 测试，再按反馈迭代；同一批次只做一次最终部署，避免每个小改动都重新打包。
- 无法通过夹具可靠验证的平台 DOM 或登录态行为，优先用当前真实页面校准，不根据旧文档猜测。
- 解释跟随问题本身：Syc 问“为什么”时说明机制，问“怎么改”时给出修改与结果；不额外安排课程、作业或术语学习。

安全门禁、dirty 仓库保护、凭据/隐私保护、破坏性操作确认、不自动提交/推送/创建 remote——**任何分级都不得削弱**。

## 完成标准

- 请求的行为已经实现，或诊断问题已经得到有证据的结论。
- 与风险相称的自动化检查通过；无法自动验证的部分明确给出一次实际验收结果或待验证项。
- 没有把已知阻断项隐藏成“已完成”；任务外的既有失败与本次回归分开说明。
- 只有产品范围、架构、合同、部署方式或长期决策发生变化时才更新对应文档；普通 bug 修复不以补文档作为关闭条件。
- 完成不依赖 Syc 阅读教程、理解术语、完成跟做或维护额外学习记录。

## 执行效率与本机稳定性

- 改动集中、边界清晰的任务由当前 Agent 直接完成；不要为了“分工”增加任务包、代理报告和重复验收。
- 不把完整子 Agent 日志、长 JSONL 或无关命令输出灌入上下文；只读取完成判断所需的最小结果。
- 不设置高频轮询或哨兵。长任务使用低频状态检查，并在状态变化或完成时更新。
- 网络和浏览器行为必须在当前实际运行环境中验证；不要把 Claude、Codex、终端或 App 之间不同的代理/DNS 表现互相当成已证明事实。
- 回复保持简洁。一个开发批次完成后用交接文档保留必要状态；后续会话按需读取，不复制整段历史。

### Mac mini 远程执行提醒

- 开始开发任务时，如果任务可能明显受益于额外 CPU、内存、磁盘或长时间后台运行，主动提醒 Syc：已配置 Mac mini 远程执行节点，可通过 `ssh mac-mini` 使用；同时说明本次是否建议使用以及原因。
- 重点提醒场景包括：SwiftPM 编译或测试、App 打包、媒体转码或转录、大型依赖安装、数据处理、长时间脚本，以及本机内存或磁盘压力较高时。简单编辑、轻量检查和必须在当前 App/UI 环境验收的任务不反复提醒。
- 提醒不等于自动转移任务。实际使用 Mac mini 前，先验证 Tailscale/SSH、远端工具链、项目路径、分支、commit 和 dirty 状态；未经 Syc 确认，不自动同步未提交文件、凭据、Keychain、`.env` 或其他敏感配置。
- Mac mini 是独立执行节点，不是本机内存扩展。远程执行需要独立 checkout 或 worktree；结果通过 Git、`rsync`、`scp` 或明确文件交接取回。
- LinkDigest 的 SwiftPM 构建和测试仍严格串行；不得在 MacBook Pro 与 Mac mini 上同时运行针对同一任务的 SwiftPM 构建。真实 App、浏览器扩展和运行时网络验收仍在对应的实际客户端环境完成。

### SwiftPM 构建纪律

- 启动任何 `swift build`、`swift test` 或打包任务前，用 `ps auxww | rg 'swift|swift-frontend'` 确认没有仍在运行的 SwiftPM 构建；必须保留 `ww`，避免长命令行被截断。
- 同一时间只允许一个 SwiftPM 构建或测试进程。不要把前一个任务转到后台后再启动新的构建。
- 主 `swift-build` 进程低 CPU 不代表挂起；先看 `swift-frontend` 是否仍在工作。构建应尽量自然结束，不因短暂无输出而超时强杀。
- 若确认无 frontend、长时间无新输出且构建确实失去进展，先停止并报告诊断；涉及清理 `.build/build.db*` 时按破坏性操作边界处理，不自动删除。

## 产品硬约束

- local-first：P0 的链接、原文、摘要和历史默认只保存在本机。
- macOS-native：P0 桌面 APP 使用 Swift + SwiftUI，只有明确的系统能力缺口才局部桥接 AppKit；不得为未验证的 Windows 需求改回 Web 桌面壳。
- Chromium-first：浏览器扩展优先支持 Chrome、Brave、Edge；Safari 不进入 P0。
- cross-language contract：Swift APP 与 TypeScript 扩展只通过版本化、语言中立的 JSON 合同交接；TypeScript interface 不能单独充当跨语言真相源。
- 当前页面优先：登录受限页面先读取用户已打开、已渲染的 DOM，不默认导入完整浏览器 Cookie。
- Provider-neutral：不得绑定单一第三方端点；至少支持自定义 OpenAI-compatible Base URL、API Key、模型名和 API 模式。
- 可解释失败：抓取、模型、网络、权限和平台限制必须分层呈现，不能只显示“失败”。
- 用户数据可迁移：历史和结果至少支持 Markdown、纯文本和 JSON 导出。
- cloud-deferred：账号、同步、托管模型、服务端与百万容量实施不进入 P0；远期文档不得反向扩大当前范围。

## 安全与合规

- 永远不要把 API Key、Cookie、Token、私钥或真实账号数据写入仓库、日志、截图和测试夹具。
- Cookie 权限必须按当前域名、用户主动触发、可撤销、短时使用；不得静默读取整个浏览器 Cookie 数据库。
- 不实现批量账号采集、绕过付费墙、绕过验证码或规避平台访问控制。
- 使用第三方开源代码前核对许可证；GPL、AGPL 和“仅限非商业学习”代码不得未经评估直接合入准备商业上架的产品。
- 购买服务器、域名、证书、签名服务，创建账号，提交商店或发布内容前必须再次获得 Syc 确认。

## 工程规则

- 先实现一条端到端可用链路，再扩平台。
- 视觉、主题、图标和排版改动优先遵循 Syc 提供的参考图与现有设计令牌。参考标准足以判断时直接实现；会明显改变视觉方向但标准缺失时，先确认关键选择。
- 图形素材默认只做扶正、缩放、裁切、换底等修正，不自行重绘；需要重绘时先获得明确授权。主题色值统一收口在 `AppearanceTheme.swift`，不要在各视图散落近似色。
- 新增或扩展内容来源必须遵循 `docs/SOURCE_ADAPTER_STANDARD.md`，以抖音单条内容和 X 单帖的“抓取 → 交接 → 本地化 → 展示 → 真机复抓”链路为质量基线。
- 共享协议先定义类型和失败语义，再连接桌面端、扩展和提取器。
- 新增平台适配器必须带至少一个脱敏夹具和成功、登录受限、平台变化三个测试场景。
- 依赖当前平台行为的结论先现场验证；不根据旧文档承诺成功率。
- 修改代码后运行与风险相称的 lint、类型检查、单元测试和端到端测试。
- 未经明确要求，不自动提交、推送、创建 remote、发布构建或安装系统级依赖。

## 文档归属

- `docs/PRD.md`：产品范围、状态、验收标准。
- `docs/ARCHITECTURE.md`：当前架构和组件边界。
- `docs/SOURCE_ADAPTER_STANDARD.md`：新增或扩展内容来源时的端到端质量基线。
- `docs/交接文档-*.md`：当前批次状态、已知问题、运行与接手说明。
- Project Brain：耐久决策与历史反转；必须通过 Brain CLI 读写，禁止手改 `brain/`。
