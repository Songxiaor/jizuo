{
  "name": "@linkdigest/browser-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt dev",
    "prepare": "wxt prepare",
    "generate:validator": "node scripts/generate-contract-validator.mjs",
    "check:validator": "node scripts/generate-contract-validator.mjs --check",
    "prebuild": "wxt prepare && pnpm generate:validator",
    "pretypecheck": "wxt prepare && pnpm generate:validator",
    "pretest": "wxt prepare && pnpm generate:validator",
    "build": "wxt build",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {"@linkdigest/shared": "workspace:*", "ajv": "8.18.0", "ajv-formats": "3.0.1"},
  "devDependencies": {"wxt": "0.20.27", "vitest": "4.1.10", "typescript": "6.0.3"}
}
