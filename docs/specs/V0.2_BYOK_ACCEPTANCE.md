# V0.2 BYOK 工程验收记录

> 状态：2026-07-14 工程验收完成；2026-07-15 已按 MAS-first 路线重新归类为可复用基线。该结论只覆盖本地单 Provider BYOK 配置与当前内存运行链路，不表示独立 Mac App、V0.1 浏览器矩阵或发布准备已经完成。当前接续顺序见 `docs/specs/MAS_FIRST_CONTINUATION.md`。

## 1. 当前可验收链路

```text
当前已捕获正文
  → 单个 ProviderProfile + Keychain API Key
  → ModelRunOrchestrator
  → OpenAICompatibleProvider / Chat Completions SSE
  → RunState
  → SwiftUI 总结 / 翻译 / 停止 / 完成 / 不完整 / 失败
```

| 任务 | 已完成能力 | 主要证据 |
|---|---|---|
| A | 非敏感 ProviderProfile、UserDefaults adapter、Keychain secret、staged save、配置 UI | Core/Adapter/ViewModel tests；隔离 Keychain service |
| B | URL 安全拼接、Chat Completions streaming、SSE、稳定 Provider 错误、有界重试、显式活动流取消 | Network.framework loopback fake server |
| C | summarize/translate intent、当前正文 prompt、RunState、Orchestrator、stop → Provider 取消交接、stale-run 隔离、streaming UI | Core 与 App fake-provider tests |
| D | 统一中文错误恢复、未知错误防回显、API Key redaction、secret scan、状态与验收文档收口 | 错误矩阵测试、sentinel tests、`pnpm secret:check` |

## 2. 错误与恢复语义

- 22 个当前 stable code 都通过同一个 App 层错误目录取得 `message` 与 `recoveryAction`，SwiftUI 不直接展示内部 code 或未知输入。
- 401 提示更新 API Key；429 提示稍后重试或更换模型服务；5xx 提示 Provider 暂时不可用并稍后重试。
- 协议不兼容提示检查 Base URL 是否为 OpenAI-compatible Chat Completions API root。
- 没有 partial 的失败进入 `failed`；已有 partial 的失败进入 `incomplete` 并明确显示“结果不完整”。
- 用户停止显示“用户已停止，结果不完整”，并继续保留已产生的部分文本。
- 未配置、没有 capture、空正文、配置存取和 Keychain 失败都有下一步动作。

## 3. Secret hygiene 证据

- API Key 持久化只进入 Keychain；ProviderProfile 和 UserDefaults 只保存非敏感字段与 opaque reference。
- API Key 只在 `loadCredentials → ModelRunOrchestrator → ModelProvider.stream` 的局部请求生命周期内短时存在。
- fake server 与 fake provider 只记录 Authorization 是否存在/匹配或 Key 是否存在，不保存 header value。
- `ModelProviderFailure.description` 只输出 stable code；用户文案不接受 Provider 原始 body、Header、完整 URL 或 `safeDetail`。
- Orchestrator 在 delta 进入 RunState 前对本次 API Key 做精确 redaction；测试覆盖成功结果和 incomplete partial 两条路径。
- 随机 sentinel 测试覆盖 RunState description、ViewModel 可见文本、用户错误文案、fake records、ProviderProfile 编码和 UserDefaults 原始数据。
- `pnpm secret:check` 静态检查高置信 secret、生产日志中的 secret/header/body 可疑形状、`@Published`/`@Observable` 与 RunState 的 secret-shaped 字段、fixture/snapshot、未知 code 的可见 UI sink 和 UserDefaults adapter；命中时只打印规则名与路径，不打印疑似值。`sentinel-<UUID>` 和 `not-a-real-key` 这类明显测试标记不会被当成真实 Key pattern。

## 4. 自动验证摘要

2026-07-14 本机执行：

- `swift test`：46 项通过，0 失败。
- `pnpm swift:test`：46 项通过，0 失败。
- `swift build -c debug` / `swift build -c release`：通过。
- `pnpm swift:build:debug` / `pnpm swift:build:release`：通过。
- `pnpm xcode:build`：LinkDigestApp 与 LinkDigestNativeHost 的 Debug/Release 四项通过。
- `pnpm secret:check`：`secret-hygiene: OK`。
- `./scripts/doctor`：`PASS=51 WARN=2 FAIL=0`；两项 WARN 是仓库尚无首次 commit、工作树未提交，均不由本任务自动处理。

Fake server 继续覆盖：path prefix、重复 endpoint 拒绝、method/header/body、多个 delta、`[DONE]`、401/404/413、429/5xx 三次总尝试、`Retry-After` 10 秒上限、delta 后断流、malformed stream、错误 Content-Type、显式取消后 producer task 在 500 ms 内退出且无迟到 delta，以及 secret hygiene。

## 5. 明确未完成

- App 内粘贴文字、公开 URL 与不依赖浏览器扩展的独立输入闭环。
- 发布级 MAS target、App Sandbox、entitlements 与 sandboxed Release 行为证据。
- 扩展到 sandboxed App 的安全 loopback bridge；当前 fake server 的 loopback 只用于 Provider adapter 测试。
- SQLite、历史、长期任务列表与持久化结果。
- Markdown/TXT/JSON 导出。
- Q&A、自动分块/MapReduce、多 Provider、Responses API、Ollama。
- 单独“测试连接”按钮；V0.2 当前通过总结/翻译路径验证连接，不影响本阶段工程完成标准。
- 真实 Provider 自动测试或手工兼容抽样。
- Edge 真实浏览器验收；V0.1 浏览器矩阵仍未关闭，但它不再阻塞 MAS 独立 App 闭环。
- 正式 Host 稳定目录、Developer ID 签名、公证、发布包和正式视觉品牌。
- 第一次向真实 Provider 发送正文前的数据去向提示尚未接入 UI；当前自动验收只使用 fake server，因此这属于发布/真实抽样前缺口。
- 云端、账号、同步与托管模型。

## 6. 当前限制与恢复

- 没有真实 Provider 抽样；如需验证 1–2 个真实端点，必须由 Syc 单独授权，并使用专用测试 Key 与非私人正文。
- 旧 Keychain item 删除仍是 staged save 完成后的 best-effort；删除失败可能留下 orphan，但不会让新 profile 指向不存在的 secret。清理队列或维护入口留给后续维护任务。
- 翻译目标语言当前固定为简体中文；V0.2 没有目标语言选择控件。
- 数字形式 `Retry-After` 已限制最多 10 秒；HTTP-date 当前回退到可注入 backoff。
- Edge 未完成时，V0.2 工程证据不能被用来宣称 V0.1 浏览器矩阵完整；同样，现有 Native Host 证据不能被用来宣称 MAS bridge 可发布。
- 单独连接测试 UI 若后续实现，必须由 Application service 短时读取 Key，不得让 SwiftUI ViewModel 直接访问 Keychain 或持有完整 Key。
- 当前 `/tmp` Host 路径只用于 V0.1 测试与本地进程交接，不是签名、公证或发布方案。

## 7. 建议下一步

以下是建议顺序，不表示任何一项已经完成：

1. **MAS Sandbox gate**：建立发布级 target、最小 entitlements 与 sandboxed Release 证据；不签名、不提交商店。
2. **Mac App 独立输入**：实现粘贴文字与公开 URL，把内容交给本页已经验收的 BYOK 组件。
3. **SQLite、历史、删除与导出**：持久化 Task/ContentSnapshot/Run/Artifact，并提供 Markdown/TXT/JSON 逃生口。
4. **真实 Provider 前置维护**：补齐数据去向提示；测试连接与 Keychain orphan 入口按独立 Issue 决定。真实抽样仍需 Syc 单独授权。
5. **条件式扩展 bridge**：只有独立 App 闭环通过后才验证安全 loopback；失败则首发不带扩展。

Edge、稳定 Native Host 与公证 DMG 继续作为未来浏览器增强/分发候选，不进入上述依赖链。完整 Issue 顺序与验收见 `docs/specs/MAS_FIRST_CONTINUATION.md`。
